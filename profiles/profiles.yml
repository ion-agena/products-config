server:
  port: 8989
feign.client.config.default.loggerLevel: full
logging:
  level:
    com.agenatech: INFO
keycloack:
  url: ${keycloak-client.server-url}
  admin-secret: '{cipher}b5615e05d6ff862dcd8b8c022c7a51486170d584fdc938150a8f7c9c299bd4b35b45c503a09504813a92cfbe96bb720a49b1d10f17bcb70e2c8fecdc48506908'
  cli-token-uri: auth/realms/master/protocol/openid-connect/token
  users-uri: auth/admin/realms/${keycloak-client.realm}/users
  email-actions-uri: auth/admin/realms/${keycloak-client.realm}/users/{id}/execute-actions-email
keycloak-client:
  server-url: http://localhost:8085
  realm: spring-micro-main
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${keycloak-client.server-url}/auth/realms/${keycloak-client.realm}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        #        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        cache.use_second_level_cache: true
        cache.region.factory_class: org.hibernate.cache.ehcache.EhCacheRegionFactory
        cache.queries: true
management:
  endpoints:
    web:
      exposure:
        include: '*'

